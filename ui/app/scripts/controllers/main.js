'use strict';

angular.module('4sh-workshops-pollApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'RESTX',
      'Twitter Bootstrap',
      'AngularJS',
      'Karma'
    ];
  });
